
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "course-review-form-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis,reg"

serviceAccountName: course-review-form-reg

cluster: es-test